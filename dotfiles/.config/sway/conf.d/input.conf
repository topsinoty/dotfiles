# Match touchpads by type so the preference survives hardware identifier changes.
input type:touchpad {
    natural_scroll enabled
}

input type:keyboard {
    xkb_layout us,ee
    xkb_options grp:alt_shift_toggle
}
