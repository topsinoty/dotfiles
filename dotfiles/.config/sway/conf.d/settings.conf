# Variables that must exist before Fedora's stock keybindings are parsed.
# The stock $menu assignment is omitted from the generated base.
set $menu fuzzel

# Preserve breathing room without replacing Fedora's layout behavior.
gaps inner 3px
gaps outer 2px
