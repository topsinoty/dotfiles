#!/bin/sh

# Preserve the current clipboard when the picker is dismissed without a choice.
set -eu

entry=$(cliphist list | fuzzel --dmenu --prompt="CLIP> ") || exit 0
[ -n "$entry" ] || exit 0

printf '%s\n' "$entry" | cliphist decode | wl-copy
